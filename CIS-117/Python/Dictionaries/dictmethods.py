# Dictionary Methods

#dict.clear() method which clears the dictionary

#dict.copy() method that copies the dictionary and doesn't modify 
# the original

# the fromkeys method just creates a new dict from given sequence of elements given by the user.

# items method returning pairs from a dict as a tuple pair in a dict

#update method adds another dict to the first dict

myDict = {"name": "Lynn", "age": 17, "address": "California"}

for key in myDict:
    print(myDict[key])

#print(sorted(MyDict, reverse=True))

#print(sorted(MyDict, key=len))


